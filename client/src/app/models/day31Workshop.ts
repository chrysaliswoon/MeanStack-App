export interface Inventory {
    id: number
    name: string
    imageUrl: string
    
}

export interface CustomerSelection {
    id: number
    name: string
    imageUrl: string
    quantity: number
}