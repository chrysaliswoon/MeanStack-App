# MEAN Stack Website

## Terminal Commands

Generate component:

```typescript
ng g c <component name> --flat --module app.module
```

Run Angular Client:

```
ng serve -o
```

Run SpringBoot Server:

```
mvn spring-boot:run
```